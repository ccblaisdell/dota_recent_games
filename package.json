{
  "scripts": {
    "deploy": "yarn build && netlifyctl deploy",
    "dev": "next",
    "build": "next build && next export",
    "start": "next start"
  },
  "dependencies": {
    "axios": "^0.17.0",
    "next": "^4.1.4",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "styled-components": "^2.2.3"
  }
}
